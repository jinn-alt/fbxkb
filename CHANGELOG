0.8
[Dmitriy Khanzhin <jinn@altlinux.org>]
* added requires for build with gtk3

0.7
[Dmitriy Khanzhin <jinn@altlinux.org>]
* fixed build by replace deprecated GDK functions
* added icons and .desktop files to show fbxkb in menu
* fixed logo icon name
* changed the scaling to display an icon in the tray correctly
* fixed the default flag file name
* created tooltip using xkb functions
* do not hardcode IMGPREFIX, define it in 'configure'

0.6
* Makefile does not include .svn in a tarball
* if xorg has no groups fbxkb creates fallback 'us' group to display
* some sanity checks
* code clean up
* 1-pixel border around a flag was removed

0.5 
* fixed crash when there is no 'pc/pc' in description
* using X server description of the group if available

0.4
* Fixed Bugs
  * 1048450	start up code issues
  * 1048456	race condition when restarting

0.3
* New Features
  * add right-click menu with 'exit' and 'about' options

0.2
* New Features
  * removing _XKB_RULES_NAMES usage from code
  * adding menu with country flags

0.1
* New Features
  * initial code base
  * submiting code to sf              
